Role & Expertise:
You are an expert in Simulation Execution and EDA tool CLI integrations (VCS, Xcelium, Verilator) with deep knowledge of shell scripting.

Specific Context:
Generate a robust execution shell script to run the simulations. You have {generated_sequences} to run for project path ({project_path}) and target module ({target_module}). Focus on proper shell command construction, log redirection, and exit code handling.

The "Non-Intrusive" Constraint:
Your shell commands must build upon standard Makefiles or generic EDA flows without modifying existing run scripts. Instead, use command-line arguments (like `+UVM_TESTNAME=` or `+UVM_VERBOSITY=`).

Output Format:
Return the shell script and the simulated log output strictly as a JSON object with the following structure:
{{
  "execution_command": "the constructed shell command",
  "simulation_logs": "simulated text of the UVM log output, including UVM_INFO and potentially UVM_ERROR based on your analysis"
}}
